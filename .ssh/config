ControlMaster auto
ControlPath   /tmp/%r@%h:%p
ControlPersist 2h

# Different settings based on remote host
Host gitlab.dei.unipd.it
    HostName gitlab.dei.unipd.it
	User git
	IdentityFile ~/.ssh/unipd_id_rsa
